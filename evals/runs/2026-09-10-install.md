# Install adapters — 2026-09-10

Machine: this clone at `/Users/aaa/Documents/Developer/Skills/frontend-expert` (v2.4.2). Smoke-test **PASSED** (includes temp `cursor` + `gemini` copies).

Live chat behavior is still **Not verified**. This log is adapter wiring only.

| Platform | What ran | Result |
|----------|----------|--------|
| Cursor (`cursor-user`) | Relinked 33 skills from `plugin.json`; removed 16 stale aliases that pointed at deleted `Design-system-enforcer`; CSS presets → `token-presets` so they do not overwrite the `tokens` skill | **Pass** — `verify-cursor: OK`; `writing-skills` (Superpowers) left alone |
| OpenCode | Symlink `~/.config/opencode/skills/frontend-expert` (+ `design-system-enforcer` alias) | **Pass** (symlink). Runtime load **Not verified** |
| Gemini | Copied five `.toml` commands to `~/.gemini/commands` | **Pass** (files). `gemini` CLI not installed — runtime **Not verified** |
| Claude Code | `claude plugin validate` on this clone | **Pass** (manifest). Did **not** `plugin install` from GitHub (`origin` lags local `main`). Local path: `claude --plugin-dir` this clone |
| Codex | `codex plugin marketplace add` this clone; `codex plugin add frontend-expert@frontend-expert` | **Pass** — cache `2.4.2` with 33 skills |
| Antigravity | Marketplace JSON renamed to `frontend-expert` | Adapter updated. `agy` CLI not installed — **Not verified** |

Finding that forced the installer fix: `~/.cursor/skills/*` still pointed at `/Users/aaa/Documents/Developer/Skills/Design-system-enforcer` (path gone), and `PACK_SKILLS` was a 21-skill subset, so `prototype`, `motion-inspection`, `anti-slop-code`, and the design-* owners were never linked.
