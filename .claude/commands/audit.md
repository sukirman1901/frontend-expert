---
description: Alias of /design — design system audit
---

Same as `/design`. Invoke the design-reviewer agent and follow `.claude/commands/design.md`.

`$ARGUMENTS` are forwarded as `/design` arguments.
