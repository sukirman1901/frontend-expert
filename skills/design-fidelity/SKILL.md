---
name: design-fidelity
description: Use when an existing prompt explicitly requests the legacy `design-fidelity` skill name.
---
# Compatibility alias: fidelity

Deprecated compatibility alias until v2.0. Load and follow `fidelity`; do not duplicate its workflow here.
