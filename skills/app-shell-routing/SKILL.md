---
name: app-shell-routing
description: Use when an existing prompt explicitly requests the legacy `app-shell-routing` skill name.
---
# Compatibility alias: app-shell

Deprecated compatibility alias until v2.0. Load and follow `app-shell`; do not duplicate its workflow here.
