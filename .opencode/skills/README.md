# OpenCode skills

OpenCode discovers skills from the repository `skills/` directory via `AGENTS.md` and the `skill` tool.

This folder is reserved for optional OpenCode-specific skill bindings. Prefer the root `skills/` as the source of truth.
