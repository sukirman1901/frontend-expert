---
name: audit
description: Alias of /design — design system audit (AI aesthetic, tokens, a11y, performance).
---

# /audit

Same as [`/design`](./design.md). Use whichever name feels natural.

Invoke the full `/design` workflow and `design-reviewer` agent.
