---
name: audit
description: Alias of /design — design system audit (AI aesthetic, polish, tokens, a11y, performance).
---

# /audit

Same as [`/design`](./design.md), including the relevant content, typography, color, and surface owner. Use whichever name feels natural.

Invoke the full `/design` workflow and `design-reviewer` agent (includes `polish` craft checks).
