{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "if [ -f \"${CLAUDE_PLUGIN_ROOT}/hooks/session-start.sh\" ]; then bash \"${CLAUDE_PLUGIN_ROOT}/hooks/session-start.sh\"; elif [ -f \"./hooks/session-start.sh\" ]; then bash \"./hooks/session-start.sh\"; fi"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "if [ -f \"${CLAUDE_PLUGIN_ROOT}/hooks/anti-slop-scan.sh\" ]; then bash \"${CLAUDE_PLUGIN_ROOT}/hooks/anti-slop-scan.sh\"; elif [ -f \"./hooks/anti-slop-scan.sh\" ]; then bash \"./hooks/anti-slop-scan.sh\"; fi",
            "timeout": 15
          }
        ]
      }
    ]
  }
}
